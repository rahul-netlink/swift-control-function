{
  "name": "swift-control-function",
  "version": "0.1.0",
  "private": true,
  "description": "Asset-agnostic, standards-based control function for digital-asset platforms (SWIFT Hackathon 2026).",
  "license": "Apache-2.0",
  "scripts": {
    "build:contracts": "forge build --root contracts",
    "test": "forge test --root contracts --match-contract InvariantsTest -vv",
    "conformance": "forge test --root contracts --match-test test_I7_conformance -vv",
    "gas": "forge test --root contracts --match-contract GasReport -vv",
    "demo": "node scripts/demo.mjs",
    "deploy": "node scripts/deploy.mjs",
    "arc": "node scripts/arc-check.mjs"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
